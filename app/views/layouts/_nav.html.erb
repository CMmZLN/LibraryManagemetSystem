
<%
=begin%>
    This is Nav bar Admin
<%
=end%>
<% current_user = User.find(session[:user_id]) %>
<div class="row"  style="width:100%; height:100vh">
  <div class="" style="background-color: #2F5597 ">
    <div class="d-flex mt-3">
      <%= image_tag("logo.png",size: "150x100") %>
      <h2 class="text-white mt-4 mb-4">図書館管理システム</h2>
    </div>
    <div class="d-flex flex-column align-items-center text-white mt-3 gap-2 ">
      <% if current_user.acc_type == "管理者" %>
        <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="/admin/students">学生一覧</a>
      <% end %>

      <% if current_user.acc_type == "管理者" %>
        <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="/admin/books">本一覧</a>
      <% else %> 
      <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="/student/books">本一覧</a>
      <% end %>
      <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="#">借りリクエスト</a>
      <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="#">返却リクエスト</a>
      <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="#">レンタル中の本</a>
      <a class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%" href="#">プロフィール</a>
      <a  href="/logout" class="border-primary border-bottom p-2 text-decoration-none text-white fw-bold" style="width:80%">ログアウト</a>
    </div>
  </div>
</div>

